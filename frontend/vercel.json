{
  "buildCommand": "yarn install && yarn expo export --platform web",
  "outputDirectory": "dist",
  "installCommand": "yarn install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
